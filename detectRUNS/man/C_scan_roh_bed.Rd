% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{C_scan_roh_bed}
\alias{C_scan_roh_bed}
\title{Scan PLINK binary (BED/BIM/FAM) for runs of homozygosity or heterozygosity}
\usage{
C_scan_roh_bed(
  bed_path,
  bim_path,
  fam_path,
  method,
  roh_type,
  min_snps,
  max_opposite,
  max_missing,
  min_length_bp,
  max_gap,
  window_size,
  threshold,
  n_threads,
  verbose
)
}
\arguments{
\item{bed_path}{Path to the .bed file}

\item{bim_path}{Path to the .bim file}

\item{fam_path}{Path to the .fam file}

\item{method}{Integer: 0 = consecutive (Marras 2015), 1 = sliding window (Bjelland 2013)}

\item{roh_type}{Integer: 0 = ROHom, 1 = ROHet}

\item{min_snps}{Minimum SNPs in a qualifying run}

\item{max_opposite}{Max opposite-type genotypes in a run (consecutive) or window (sliding)}

\item{max_missing}{Max missing genotypes in a run (consecutive) or window (sliding)}

\item{min_length_bp}{Minimum run length in base pairs}

\item{max_gap}{Max gap between consecutive SNPs (bp); >= this breaks a run}

\item{window_size}{Window width in SNPs (method=1 only)}

\item{threshold}{Bjelland coverage ratio threshold, strictly > (method=1 only)}

\item{n_threads}{Number of OpenMP threads (1 = single-threaded)}

\item{verbose}{If TRUE, print a progress bar during scan and a summary on completion}
}
\value{
Named list: runs, summary, snp_freq, chrom_map
}
\description{
Low-level C++ entry point called by \code{scanRUNS()}.
Do not call directly; use \code{scanRUNS()} instead.
}
