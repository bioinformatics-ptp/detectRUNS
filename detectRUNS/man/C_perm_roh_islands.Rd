% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{C_perm_roh_islands}
\alias{C_perm_roh_islands}
\title{Permutation-based ROH island detection}
\usage{
C_perm_roh_islands(
  bed_path,
  bim_path,
  fam_path,
  snp_freq_r,
  method,
  roh_type,
  min_snps,
  max_opposite,
  max_missing,
  min_length_bp,
  max_gap,
  window_size,
  threshold,
  n_threads,
  n_perm,
  percentile,
  seed
)
}
\arguments{
\item{bed_path}{Path to the .bed file}

\item{bim_path}{Path to the .bim file}

\item{fam_path}{Path to the .fam file}

\item{snp_freq_r}{Named integer vector of real SNPROH counts from
\code{scanRUNS()$snp_freq}}

\item{method}{Integer: 0=consecutive, 1=sliding}

\item{roh_type}{Integer: 0=ROHom, 1=ROHet}

\item{min_snps}{Same parameter as the original scan}

\item{max_opposite}{Same parameter as the original scan}

\item{max_missing}{Same parameter as the original scan}

\item{min_length_bp}{Same parameter as the original scan}

\item{max_gap}{Same parameter as the original scan}

\item{window_size}{Sliding window width (sliding method only)}

\item{threshold}{Coverage ratio threshold (sliding method only)}

\item{n_threads}{OpenMP thread count (parallelism over permutations)}

\item{n_perm}{Number of permutations}

\item{percentile}{Quantile for threshold derivation (e.g. 0.99)}

\item{seed}{MT19937 seed; 0 = draw from random_device}
}
\value{
Named list with \code{thresholds} (named numeric vector, one per
  chromosome) and \code{is_island} (named logical vector, one per SNP).
}
\description{
Low-level C++ entry point called by \code{rohIslands()}.
Do not call directly; use \code{rohIslands()} instead.
}
