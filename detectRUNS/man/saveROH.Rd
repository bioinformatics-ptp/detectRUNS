% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary_roh.R
\name{saveROH}
\alias{saveROH}
\title{Save ROH scan results to a compact binary file}
\usage{
saveROH(result, path)
}
\arguments{
\item{result}{Named list returned by \code{scanRUNS()}.  Must contain
at least \code{$runs} and \code{$chrom_map}.}

\item{path}{Output file path (e.g. \code{"results.roh"}).}
}
\value{
Invisibly returns \code{path}.
}
\description{
Serialises the \code{$runs} table from a \code{scanRUNS()} result to a
self-contained binary file (format ROHB v1).  The file is smaller and
faster to read than CSV and can be loaded back with \code{loadROH()}.
}
\examples{
\dontrun{
bedFile <- system.file("extdata", "Kijas2016_Sheep_subset.bed",
                        package = "detectRUNS")
roh <- scanRUNS(bedFile, method = "sliding", minSNP = 15,
                minLengthBps = 100000)
saveROH(roh, tempfile(fileext = ".roh"))
}
}
\seealso{
\code{\link{loadROH}}, \code{\link{scanRUNS}}
}
