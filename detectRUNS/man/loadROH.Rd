% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary_roh.R
\name{loadROH}
\alias{loadROH}
\title{Load ROH scan results from a binary file}
\usage{
loadROH(path)
}
\arguments{
\item{path}{Path to a \code{.roh} binary file.}
}
\value{
A named list with element \code{$runs} (a \code{data.table}).
}
\description{
Reads a binary file written by \code{saveROH()} and returns a named list
whose \code{$runs} element is a \code{data.table} in the same format as
\code{scanRUNS()$runs} (columns: group, id, chrom, nSNP, from, to,
lengthBps).
}
\examples{
\dontrun{
bedFile <- system.file("extdata", "Kijas2016_Sheep_subset.bed",
                        package = "detectRUNS")
roh     <- scanRUNS(bedFile, method = "sliding", minSNP = 15,
                    minLengthBps = 100000)
rohFile <- tempfile(fileext = ".roh")
saveROH(roh, rohFile)
roh2 <- loadROH(rohFile)
nrow(roh2$runs)
}
}
\seealso{
\code{\link{saveROH}}, \code{\link{scanRUNS}}
}
