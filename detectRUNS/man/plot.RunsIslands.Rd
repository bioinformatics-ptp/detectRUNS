% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runsIslands.R
\name{plot.RunsIslands}
\alias{plot.RunsIslands}
\title{Manhattan plot of ROH island detection results}
\usage{
\method{plot}{RunsIslands}(
  x,
  col_island = "firebrick",
  col_snp = c("grey60", "grey80"),
  col_threshold = "steelblue",
  title = "Runs Island Detection",
  pt_size = 0.6,
  pt_alpha = 0.8,
  ...
)
}
\arguments{
\item{x}{An \code{RunsIslands} object returned by
\code{\link{runsIslands}}.}

\item{col_island}{Colour for island SNPs.  Default \code{"firebrick"}.}

\item{col_snp}{Two-element character vector of alternating colours for
non-island SNPs (one per chromosome, alternating).
Default \code{c("grey60", "grey80")}.}

\item{col_threshold}{Colour for the per-chromosome threshold lines.
Default \code{"steelblue"}.}

\item{title}{Plot title.  Default \code{"Runs Island Detection"}.}

\item{pt_size}{Point size for SNPs.  Default \code{0.6}.}

\item{pt_alpha}{Point transparency.  Default \code{0.8}.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot2} object (invisible).  The plot is printed as a
  side-effect.
}
\description{
Plots SNPROH frequency (percentage of individuals with a given SNP inside a
ROH) across the genome, with chromosome-specific permutation thresholds
shown as dashed lines and island SNPs highlighted in a distinct colour.
The plot mirrors Fig. 1 of Falchi et al. (2026, \emph{BMC Genomics}).
}
\examples{
\dontrun{
bedFile <- system.file("extdata", "Kijas2016_Sheep_subset.bed",
                        package = "detectRUNS")
roh     <- scanRUNS(bedFile, method = "consecutive", minSNP = 15,
                    maxOpp = 1, maxMiss = 1, minLengthBps = 100000)
islands <- runsIslands(roh, n_perm = 100, seed = 42)
plot(islands)
}
}
\seealso{
\code{\link{runsIslands}}, \code{\link{summary.RunsIslands}}
}
